# 🐳 Быстрый старт с Docker

## Что нужно установить

1. **Docker Desktop** — скачайте с [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop/)
   - Windows: установите .exe файл
   - macOS: установите .dmg файл или через Homebrew: `brew install --cask docker`
   - Linux: `sudo apt-get install docker.io docker-compose`

2. **Ollama** — скачайте с [ollama.ai](https://ollama.ai)
   - Установите и запустите Ollama
   - Скачайте модель: `ollama pull gemma:latest`

## Запуск (3 шага)

```bash
# 1. Перейдите в директорию проекта
cd AIBusinessAnalyst

# 2. Убедитесь, что Ollama запущен (в отдельном терминале)
ollama serve

# 3. Запустите приложение (с автоматической очисткой Docker)
./docker-run.sh
```

**Или без автоматической очистки:**
```bash
docker-compose up --build
```

## Откройте в браузере

**http://localhost:8501**

## Остановка

Нажмите `Ctrl+C` в терминале или:
```bash
docker-compose down
```

## Полезные команды

```bash
# Запуск с автоматической очисткой (рекомендуется)
./docker-run.sh

# Пересобрать контейнер (без очистки)
docker-compose up --build

# Запустить в фоне
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Полная очистка Docker вручную
docker system prune -af --volumes
```

## Решение проблем

### Порт 8501 занят
Измените в `docker-compose.yml`:
```yaml
ports:
  - "8502:8501"  # Используйте другой порт
```

### Ollama недоступен
Убедитесь, что Ollama запущен на хосте. На Linux может потребоваться добавить в `docker-compose.yml`:
```yaml
extra_hosts:
  - "host.docker.internal:host-gateway"
```

### Контейнер не запускается
```bash
# Проверьте логи
docker-compose logs

# Пересоберите с нуля
docker-compose build --no-cache
docker-compose up
```

## Что внутри контейнера?

- ✅ Python 3.10
- ✅ Java 17 (для PlantUML)
- ✅ Все Python библиотеки из requirements.txt
- ✅ PlantUML JAR файл
- ✅ Все необходимые системные библиотеки

**Вам НЕ нужно устанавливать ничего из этого вручную!**

